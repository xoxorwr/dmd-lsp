# dmd-lsp

A minimal, dmd-frontend-native language server for D. Diagnostics,
completion, signature help and lint run on real `dmd` semantic, used
read-only as a library — no DCD, no libdparse heuristics.

![Completion of declarations generated by a CTFE string mixin](docs/mixin.png)

## Get it

Prebuilt nightly (Linux x64, macOS arm64/x64):
**[Releases → nightly](https://github.com/xoxorwr/dmd-lsp/releases/tag/nightly)**

- `dmd-lsp-<platform>.tar.gz` — standalone server binary (also `--check`).
- `dmd-lsp.vsix` — VS Code extension (fetches and auto-updates the binary).
- `SHA256SUMS`.

Windows is not supported yet (the worker is POSIX-only); build from source or
point `dmdLsp.serverPath` at your own build.

More in [docs/releases.md](docs/releases.md).

## Build

```sh
make            # -> ./dmd-lsp, from the vendored src/dmd/
make DC=ldmd2   # same, with LDC's dmd-compatible driver (smaller binary)
make check      # struct-only guard + batch fixtures + LSP regression suite
```

Needs a D compiler (DMD 2.113 line, or LDC ≥ 1.42 whose frontend covers it).
The dmd frontend is vendored under `src/dmd/` so the build is
self-contained; refresh it with `make vendor` — see
[docs/vendoring.md](docs/vendoring.md).

## Usage

CLI:

```sh
./dmd-lsp --check FILE...          # batch diagnostics (exit 1 on errors)
./dmd-lsp --stdio                  # LSP over stdio (default)
./dmd-lsp --import=DIR...          # module import paths (-I)
./dmd-lsp --string-import=DIR...   # string import paths (-J, import("..."))
./dmd-lsp --flag=-preview=NAME     # pass a supported dmd flag
./dmd-lsp --debounce-ms=N          # diagnostics idle delay (default 300)
./dmd-lsp --version
```

Editor: point any LSP client at `dmd-lsp` for `.d`/`.di`. Supported:
`initialize`, `textDocument/{didOpen,didChange,didClose,didSave}`,
`completion` (LSP 3.17 `labelDetails` when the client opts in),
`signatureHelp`, `codeAction` (remove unused import), `publishDiagnostics`.

Configuration (precedence: editor settings → CLI → `dls.json` → builtin
stdlib defaults):

```jsonc
// <workspace>/dls.json — project truth (relative paths resolve to the root)
{
  "importPaths": ["src/", "sandbox/"],
  "stringImportPaths": ["views/"],   // for import("...") files (-J)
  "flags": ["-preview=rvaluerefparam", "-preview=bitfields", "-betterC"],
  "debounceMs": 300
}
```

Editor `initializationOptions`/settings take the same keys (bare, or under
`"dmd-lsp"`/`"d"`/`"D"`). Mirrored dmd flags matter: previews change overload
resolution, so a build that omits one here shows spurious errors. `dls.json`
loads at `initialize` and on save; the VS Code extension can write one via
**dmd-lsp: Create dls.json**.

> **dub** projects (`dub.json`/`dub.sdl`) are not auto-configured yet — list
> the dependency import paths in `dls.json` for now. dub support (reading
> `dub describe` and watching the manifest) is planned; see
> [docs/upstream.md](docs/upstream.md).

## How it works

Analysis runs in a **forked worker, one dmd universe per worker**: the front
end holds no dmd state, a worker does exactly one full build and serves cache
hits from it, and on any input change it respawns, so nothing accumulates
(dmd's process globals cannot be reset in-process). Inside a worker,
identical inputs (root text + dep disk bytes + config) hit the universe cache
for free; diagnostics are debounced, and completion/signature help reuse the
live universe. While you type broken code, completion analyses a neutralised
variant of the buffer (keyed on the real text) so dmd does not collapse the
function body and drop local types.

Internals, measurements and limitations: [docs/design.md](docs/design.md).

## Docs

- [design.md](docs/design.md) — internals, conventions, limitations, status
- [vendoring.md](docs/vendoring.md) — vendored dmd closure, Makefile
- [releases.md](docs/releases.md) — nightly CI, VS Code extension
- [upstream.md](docs/upstream.md) — patches in `../dmd`, roadmap
- [findings.md](docs/findings.md) — in-process memory/GC investigation
