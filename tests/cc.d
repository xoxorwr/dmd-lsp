module cc;

struct Point
{
    int x;
    int y;
    string name;
}

int dist(Point p, int extra)
{
    int doubled = extra * 2;
    Point origin;
    origin.x = 1;
    string s = "notinstring";
    // notincomment
    auto q = dou;
    auto a = origin.x;
    auto b = p.y;
    auto c = Point.x;
    if (auto dd = doubled)
    {
        dd;
    }
    return doubled + p.x;
}
